import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";

import { Header } from "./Header";
import { IconButton } from "../IconButton/IconButton";
import { Progress } from "../Progress/Progress";

<Meta title="Common | Header" />

# Preview

<Preview>
  <Story name="Home">
    <Header button={<IconButton icon="settings" onClick={() => {}} />} />
  </Story>
</Preview>

<Preview>
  <Story name="Question">
    <Header title="Entertainment: Video Games">
      <Progress current={4} total={20} />
    </Header>
  </Story>
</Preview>

<Preview>
  <Story name="Results">
    <Header title="Well Done!" button={<IconButton icon="home" onClick={() => {}} />}>
      <Progress current={8} total={10}>
        <div>
          YOU ANSWERED{" "}
          <strong>
            8/10
          </strong>{" "}
          QUESTIONS CORRECTLY
        </div>
      </Progress>
    </Header>
  </Story>
</Preview>

# Props

<Props of={Header} />
